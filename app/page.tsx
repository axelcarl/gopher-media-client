import Posts from "@/components/Posts";
import { FC } from "react";

const Page: FC = ({ }) => {

    return (
        <div className="pt-8">
            <Posts />
        </div>
    );
};

export default Page;
